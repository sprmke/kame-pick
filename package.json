{
  "name": "job-applicants-analyzer",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": ".venv/bin/python -m uvicorn server.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "cd web && npm run dev",
    "install:all": "python -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-server.txt && cd web && npm install",
    "build:web": "cd web && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
